# InternetOfAgriculture
A Java GUI application is used as a control panel which displays the data read from the sensors and provides the user with the control over actuators connected to the Raspberry-Pi3 which runs a python 3 application. Both these read and write to a common Firebase Real-Time database and uses this as a medium to communicate.

Language/Tools: Python, Firebase, Java, Raspberry-Pi 3
